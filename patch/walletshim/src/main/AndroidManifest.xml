<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <uses-permission
        android:name="android.permission.WRITE_SECURE_SETTINGS"
        tools:ignore="ProtectedPermissions" />

    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
        <package android:name="com.google.android.apps.walletnfcrel" />
    </queries>

    <application
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="false"
        android:icon="@drawable/ic_power_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_power_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.PowerActions"
        android:usesCleartextTraffic="false">

        <!-- User-facing Material You configuration screen. -->
        <activity
            android:name=".SettingsActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            Invisible one-shot bridge for the ColorOS Wallet shortcut.
            CATEGORY_INFO is intentional: package front-door lookup prefers INFO before LAUNCHER,
            so firmware launching com.heytap.wallet by package reaches the bridge while the app icon
            continues to open SettingsActivity.
        -->
        <activity
            android:name=".BridgeActivity"
            android:excludeFromRecents="true"
            android:exported="true"
            android:finishOnTaskLaunch="true"
            android:showWhenLocked="true"
            android:taskAffinity="com.heytap.wallet.redirect"
            android:theme="@style/Theme.PowerActions.Redirect"
            android:turnScreenOn="true">
            <intent-filter android:priority="1000">
                <action android:name="${walletOpenAction}" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
            <intent-filter android:priority="1000">
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.INFO" />
            </intent-filter>
        </activity>

        <!--
            Narrow redirect: receives only window-state events from the OPPO Wallet package and
            cannot retrieve window content.
        -->
        <service
            android:name=".WalletRedirectAccessibilityService"
            android:directBootAware="true"
            android:exported="true"
            android:label="@string/accessibility_service_label"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- ColorOS may remove third-party accessibility entries after reboot. -->
        <receiver
            android:name=".AccessibilityRestoreReceiver"
            android:directBootAware="true"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.USER_UNLOCKED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
